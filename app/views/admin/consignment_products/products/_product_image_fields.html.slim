.nested-fields.well
  = f.input :image, as: :image_preview, label: false, input_html: { preview_version: :thumb, class: 'fancy' }
  = f.input :remote_image_url, as: :hidden
  = link_to_remove_association '移除這張圖片', f, class: 'btn btn-danger btn-mini'
